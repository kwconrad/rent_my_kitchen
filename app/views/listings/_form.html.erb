<!-- This has to be changed -->
<%= form_for(@kitchen, @listing) do |f| %>
  <%= select_date Date.today, prefix: :start_date %>

  <%= f.input :price_per_hour %>
  <%= f.text_field :price_per_hour %>


  <%= f.submit %>
<% end %>
